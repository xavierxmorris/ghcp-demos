# The demo subfolders are independent git repos (published separately).
# This index repo only tracks the top-level files (README, scripts, repos.txt, LICENSE).
# Glob rather than an explicit list, so newly added demos are covered automatically.
ghcp-demo-*/

# Local run logs downloaded by `gh aw audit` / `gh aw logs`.
.github/aw/logs/
